const config = {};

config.mongodb = {};
config.mongodb.userName = "username"; // replace with username for your database
config.mongodb.password = "password"; // replace with password for your database

module.exports = config;